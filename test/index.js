describe('component-ioc', () => {
    it('generates a new file containing a store that links paths to component definitions', () => {

    });

    it('transforms component imports into dependency', () => {
        
    });

    it('transforms component references into dynamic bindings', () => {

    });
});